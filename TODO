1. logic components (if, elsif, else; foreach; counter; raw; yield)
2. Cache parsed XML
3. Ability to add own logic components (Beholder.register_component(name) { |node| ... })
4. Compatibility with Rails, Waves, Sinatra, Rack